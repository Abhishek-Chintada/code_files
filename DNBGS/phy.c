// writing a c file to calculate the gravitational force between the paricles inside the box.

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

typedef struct {
    double x, y;
    double vx, vy;
    double mass;
} Particle;

void calculate(int count, float dt, Particle *p) {
       
}