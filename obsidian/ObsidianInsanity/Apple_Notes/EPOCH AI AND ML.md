IITH

Tuples are slightly faster than the lists in python.
Dictionary is a collection of key values.’
A <span style="font-size:12pt;">break</span> statement within a <span style="font-size:12pt;">for</span> loop causes the program to exit the complete loop, not just the current iteration
Indentations in python plays a key role.
Enumerate provides values and index in the loops in python language.
Numpy arrays are much faster than the general lists and tuples.
Np.arange is exclusive whereas the np.linspace is inclusive.
By using -1 as one of the arguments of the np.reshape the compiler itself infers the shape automatically by the help of the number of elements in the original array.
@ symbol can be used for matrix multiplication of the arrays of numpy.
np.linalg.inv is used to invert a matrix in python 
a.sum(axis = 0) takes the sum of the numbers ni the array a along the columns.
Similarly a.sum(axis = 1) takes the sum of the numbers In the array an along the row.
(Basic math for the ai and ml is written in notebook )
The gradient is a multi variable generalisation of the derivative.
The gradient is also a vector.
The gradient of a vector points to the direction of the steepest increase.
Probability is the likelihood of the happening of an event.
A random variable is a variable whose value depends on the result of a random experiment.
The next word prediction is a result of a ml model.
Words or sentences are converted to vector, and these vectors are called the embedding vectors.
The next word prediction is an application of the embedding vector.
Neural networks are used to make an embedded vector for a word.
The expected embedding is based on the probability weighted sum.
Basically the model assigns a probability to each and every possible next word.
Variance is used to find the spread of a random variable around the mean.
Lower the variance the better the model.
We should learn to model a relationship between the inputs and the outputs.
A lot of predictions can be approximated using a well drawn line.
(Concept of hypothesis function)
Here intercept is called the bias.
X is called the feature vector and theta is called the weight vector.
(Learn about the linear regression.)
We use errors to evaluate the hypothesis function,
Mean absolute error and mean square error.